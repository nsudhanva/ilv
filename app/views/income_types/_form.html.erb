<%= form_for(income_type) do |f| %>
  <% if income_type.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
      <div id="error_explanation">
        <h2><%= pluralize(income_type.errors.count, "error") %> prohibited this income_type from being saved:</h2>

        <ul>
        <% income_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
