<%= form_for(resident_type) do |f| %>
  <% if resident_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(resident_type.errors.count, "error") %> prohibited this resident_type from being saved:</h2>

      <ul>
      <% resident_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
