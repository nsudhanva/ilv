<%= form_for(income) do |f| %>
  <% if income.errors.any? %>
    <div class="alert">
      <div class="row">
        <div class="col s12">
          <div class="card-panel red">
            <span class="white-text">         
              <div id="error_explanation">
                <h6><%= pluralize(income.errors.count, "error") %> prohibited this income from being saved:</h6>

                <ul>
                <% income.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
                </ul>
              </div>
            </span>
          </div>
        </div>
      </div>
    </div>      
  <% end %>
  
  <div class="field">
    <%= f.label :amount %>
    <%= f.text_field :amount %>
  </div>

  <div class="field">
    <label>Select Resident: </label>
    <%= f.collection_select :resident_id, Resident.all , :id, :name, include_blank: "Select Resident" %> <br><br>
  </div>

  <div class="field">
    <%= f.label :date_time %>
    <%= f.datetime_select :date_time %>
  </div>

  <div class="field">
    <%= f.label :month %>
    <%= f.select :month, ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"], include_blank: "Select Month" %>
  </div>

  <div class="field">
    <label>Select Payment Type: </label>
    <%= f.collection_select :payment_type_id, PaymentType.all , :id, :name, include_blank: "Select Payment" %> <br><br>
  </div>

  <div class="field">
    <label>Select Income Type: </label>
    <%= f.collection_select :income_type_id, IncomeType.all , :id, :name, include_blank: "Select Income" %> <br><br>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
